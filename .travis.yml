language: rust

sudo: required

rust:
  - 1.40.0
  - nightly

matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

cache:
  cargo: true

script:
  - cargo build --verbose --all
  - cargo test --verbose --all

notifications:
  email: false
